import { InternProfile, WeeklyMetrics, WeeklyStrategists } from '../types';

export const seedProfiles: InternProfile[] = [
    // No dummy data - you'll add your interns manually through the admin panel
];

// No initial weeks - you'll create weeks manually through the admin panel
export const seedWeeklyStrategists: WeeklyStrategists[] = [];

// No initial metrics - you'll add metrics manually through the admin panel
export const seedWeeklyMetrics: WeeklyMetrics[] = [];